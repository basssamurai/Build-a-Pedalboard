<%= form_for @pedalboard do |f| %>
  <!-- Good use of a partial -->
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :photo_url %>
  <%= f.text_field :photo_url %>

  <%= f.submit %>

<% end %>
