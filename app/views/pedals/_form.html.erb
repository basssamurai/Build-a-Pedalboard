<%= form_for @pedal do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :style %>
  <%= f.text_field :style %>

  <%= f.label :photo_url %>
  <%= f.text_field :photo_url %>

  <%= f.submit %>

<% end %>
